sql:
  driver: org.h2.Driver
  url: jdbc:h2:~/test
  user: sa
  password:

queries:
  create: CREATE TABLE IF NOT EXISTS users (id integer, name varchar(100), age integer)
  truncate: TRUNCATE TABLE users
  insert: INSERT INTO users (id, name, age) VALUES (?, ?, ?)
  select: SELECT * FROM users WHERE age BETWEEN 20 AND 25
